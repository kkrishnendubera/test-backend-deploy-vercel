
{
  "version": 2,
  "builds": [
    {
      "src": "src/serverless.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "public/**", "views/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/serverless.ts" }
  ]
}